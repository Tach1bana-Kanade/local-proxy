# 局部代理项目方案

> 版本：0.1（方案阶段）  
> 日期：2026-08-02  
> 目标平台：macOS 26，Apple Silicon  
> 当前代理出口：Quickcat SOCKS5 `127.0.0.1:21080`，HTTP `127.0.0.1:21081`

## 1. 项目目标

开发一个 macOS 局部代理工具，默认所有网络请求直连；只有用户明确添加的网站或应用使用代理网络。

核心目标：

- 默认出口始终为 `DIRECT`。
- 支持按网站、域名后缀、IP/CIDR 分流。
- 支持按应用、进程名称、进程路径分流。
- 复用 Quickcat 作为上游代理，不读取或修改 Quickcat 的账号、节点订阅和内部文件。
- 支持一键启停、实时查看规则命中和连接出口。
- 工具关闭、崩溃或代理不可用时，能够安全恢复网络。
- 不安装 HTTPS 根证书，不解密用户通信内容。

## 2. 非目标

首版不包含：

- 自行实现 Shadowsocks、VLESS、Trojan、Hysteria 等代理协议。
- 节点订阅、节点测速或节点切换功能。
- 修改 Quickcat 配置文件。
- 广告过滤、内容审查或 HTTPS 中间人解密。
- Windows、Linux、iOS 和 Android 客户端。
- 面向 Mac App Store 的公开发行。

## 3. 总体架构

```text
macOS 应用网络请求
        │
        ▼
TUN 分流核心（Mihomo）
        │
        ├── 本地地址 / 局域网 / 工具自身 / Quickcat ──→ DIRECT
        │
        ├── 命中“代理应用”规则 ───────────────────────→ Quickcat
        │                                                127.0.0.1:21080
        ├── 命中“代理网站”规则 ───────────────────────→ Quickcat
        │                                                127.0.0.1:21080
        ├── 命中“强制直连”规则 ───────────────────────→ DIRECT
        │
        └── 未命中任何规则 ───────────────────────────→ DIRECT
```

项目分为三个模块：

1. **macOS 管理应用**：Swift、SwiftUI、菜单栏界面。
2. **规则与配置层**：维护用户规则并生成 Mihomo 配置。
3. **网络分流核心**：Mihomo TUN，负责流量识别和出口选择。

## 4. 技术选型

### 4.1 管理界面

- Swift 6。
- SwiftUI。
- MenuBarExtra 菜单栏入口。
- AppKit 用于应用选择器、Bundle 信息读取和必要的 macOS 集成。
- SwiftData 或 JSON 文件保存用户规则；首版建议 JSON，便于调试和导入导出。

### 4.2 网络核心

首版使用 Mihomo，不从零开发 TUN 和代理协议。

选用理由：

- 支持 macOS 和 Apple Silicon。
- 支持 TUN。
- 支持 `DOMAIN`、`DOMAIN-SUFFIX`、`IP-CIDR`。
- 支持 `PROCESS-NAME`、`PROCESS-PATH` 及通配符/正则形式。
- 支持 `MATCH,DIRECT`，可以实现默认直连。
- 支持将 SOCKS5 服务器定义为上游出口。
- 有连接日志和控制 API，便于开发可视化界面。

### 4.3 上游代理

默认使用 Quickcat：

```text
SOCKS5：127.0.0.1:21080
HTTP：  127.0.0.1:21081
```

首选 SOCKS5 `21080`，HTTP `21081` 用于备用和连通性诊断。

Quickcat 应保持：

- 已连接。
- “纯代理”模式。
- TUN 关闭。
- 系统代理关闭。
- 局域网共享关闭。

本工具开启 TUN 后，Quickcat 不应再开启自己的 TUN，避免双重接管和路由冲突。

## 5. 关键规则设计

### 5.1 规则优先级

规则必须从高到低执行：

1. 回环地址和工具内部通信：`DIRECT`。
2. Mihomo 自身流量：`DIRECT`。
3. Quickcat 自身流量：`DIRECT`。
4. 局域网和保留地址：`DIRECT`。
5. 用户设置的拒绝规则：`REJECT`。
6. 用户设置的强制直连应用/网站：`DIRECT`。
7. 用户设置的代理应用：`QUICKCAT`。
8. 用户设置的代理网站：`QUICKCAT`。
9. 未命中规则：`DIRECT`。

Quickcat 和 Mihomo 必须优先直连，否则可能产生代理循环。

### 5.2 网站规则

支持以下类型：

- 精确域名：`api.example.com`
- 域名后缀：`example.com`，同时匹配其子域名
- 通配符：`*.example.com`
- 单个 IP：`203.0.113.10`
- IPv4/IPv6 CIDR
- 强制直连例外
- 临时规则和启用/禁用状态

输入规则时需要：

- 自动去掉 `http://`、`https://`、路径和查询字符串。
- 转换为小写。
- 支持国际化域名的 IDNA/Punycode 标准化。
- 拒绝非法域名、无效 IP 和过宽的危险 CIDR。

### 5.3 应用规则

用户通过应用选择器选择 `.app`，工具读取并保存：

- 应用显示名称。
- Bundle Identifier。
- 主可执行文件路径。
- 可执行文件名称。
- 代码签名标识（可获得时）。
- 应用图标。
- 是否包含子进程。

规则匹配优先使用完整进程路径；应用更新导致路径变化时，退回到进程名称，并提示用户重新确认。

需要处理辅助进程，例如：

- Electron Helper。
- Chromium Helper。
- XPC Service。
- CLI 子进程。

首版提供“包含该应用的辅助进程”开关，但不能只凭模糊名称自动放行所有 Helper，避免误匹配其他应用。

## 6. Mihomo 配置示意

以下仅用于表达结构，实际配置由程序生成：

```yaml
mode: rule
find-process-mode: strict

tun:
  enable: true
  stack: mixed
  auto-route: true
  auto-detect-interface: true
  strict-route: true
  dns-hijack:
    - any:53

proxies:
  - name: QUICKCAT
    type: socks5
    server: 127.0.0.1
    port: 21080
    udp: true

rules:
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve

  # 工具自身和 Quickcat 的真实进程路径在安装时探测
  - PROCESS-PATH,/path/to/mihomo,DIRECT
  - PROCESS-PATH,/Applications/Quickcat.app/Contents/MacOS/Quickcat,DIRECT

  # 用户应用规则
  - PROCESS-PATH,/Applications/Example.app/Contents/MacOS/Example,QUICKCAT

  # 用户网站规则
  - DOMAIN-SUFFIX,example.com,QUICKCAT

  # 默认直连
  - MATCH,DIRECT
```

注意：必须先验证 Quickcat 的 SOCKS5 出口是否支持 UDP。如果不支持，代理规则中的 UDP 流量应阻止或允许应用回退到 TCP，不能静默误导用户。

## 7. DNS 与域名识别

网站分流的准确性依赖 DNS 和域名信息。

计划：

- TUN 模式接管普通 DNS 请求。
- 直连域名使用系统或指定的直连 DNS。
- 代理域名使用经 Quickcat 访问的 DNS，减少错误解析和泄漏。
- 使用目标域名、DNS 映射和 TLS SNI 辅助识别，不进行 TLS 解密。
- 保留 `localhost`、`.local`、局域网 DNS 和企业内网域名直连能力。

必须测试：

- DoH/DoT 应用。
- IPv6 AAAA 查询。
- Safari、Chrome、Firefox 的差异。
- QUIC/HTTP3。
- 睡眠唤醒后的 DNS 恢复。

## 8. 故障策略

工具提供两种代理故障策略：

### 防泄漏模式（默认推荐）

命中代理规则但 Quickcat 不可用时，阻止该连接。未命中规则的其他流量继续直连。

### 自动直连模式

Quickcat 不可用时，将原本命中代理规则的流量临时改为直连。

界面必须明确显示当前策略，不能在后台静默切换。

## 9. 用户界面规划

### 9.1 菜单栏

- 分流开关。
- Quickcat 状态。
- 当前代理节点是否可达。
- 当前代理连接数量。
- “全部暂停并恢复直连”。
- 打开主窗口。

### 9.2 主窗口

#### 应用规则

- 添加应用。
- 应用图标、名称和路径。
- 代理/直连策略。
- 辅助进程设置。
- 最近命中时间和流量统计。

#### 网站规则

- 添加域名/IP。
- 精确域名或后缀匹配。
- 代理/直连/拒绝。
- 分组、搜索、启用/禁用。

#### 连接监控

- 时间。
- 应用。
- 域名或目标地址。
- TCP/UDP。
- 命中的规则。
- 最终出口：`DIRECT` 或 `QUICKCAT`。

#### 设置

- Quickcat 主机和端口。
- 故障策略。
- 登录时启动。
- 日志级别和日志保留时间。
- 导入、导出规则。
- 一键恢复网络。

## 10. 安全设计

- 控制 API 只监听 `127.0.0.1`。
- 控制 API 使用随机高强度令牌。
- 配置文件权限限制为当前用户读取。
- 不保存 Quickcat 账号和节点订阅。
- 不安装根证书。
- 不记录完整 URL、请求正文、Cookie 和认证头。
- 默认日志只保存域名、应用标识、规则结果和必要错误。
- 日志支持自动轮转和一键清理。
- 所有远程规则订阅必须通过 HTTPS，并明确显示来源与更新时间。
- 首版默认关闭远程规则订阅，优先使用用户本地规则。

## 11. 权限与部署

TUN 需要系统权限。首版需要设计一个最小权限的辅助服务：

- 只负责启动、停止 Mihomo 和维护必要网络路由。
- 不提供任意命令执行接口。
- 使用固定、可验证的 Mihomo 二进制路径和哈希。
- 应用退出时正常关闭 TUN。
- 崩溃恢复时清理残留路由和 DNS 设置。
- 安装、更新和移除辅助服务时向用户解释具体变更。

如果以后改用 Apple Network Extension，需要 Apple Developer 签名、Network Extension entitlement、系统扩展审批和公证流程。首版先使用 Mihomo 验证需求，不立即投入原生 Network Extension。

## 12. 项目目录建议

```text
LocalProxy/
├── README.md
├── LICENSE
├── docs/
│   ├── architecture.md
│   ├── rule-model.md
│   ├── security.md
│   └── testing.md
├── macOSApp/
│   ├── App/
│   ├── Features/
│   │   ├── AppRules/
│   │   ├── DomainRules/
│   │   ├── Connections/
│   │   └── Settings/
│   ├── CoreBridge/
│   ├── Models/
│   └── Resources/
├── Helper/
│   ├── Service/
│   └── IPC/
├── Core/
│   ├── ConfigGenerator/
│   ├── RuleCompiler/
│   └── MihomoManager/
├── Tests/
│   ├── UnitTests/
│   ├── IntegrationTests/
│   └── NetworkTests/
└── scripts/
    ├── diagnose.sh
    └── verify-network.sh
```

## 13. 开发阶段

### 阶段 0：技术验证

交付物：命令行原型和测试报告。

- 验证 Mihomo 能连接 Quickcat `21080`。
- 验证 `MATCH,DIRECT` 默认直连。
- 验证域名规则走 Quickcat。
- 验证进程名称和进程路径规则。
- 验证 Quickcat 自身不会进入代理循环。
- 验证 TCP、UDP、IPv4、IPv6。
- 验证关闭核心后网络完全恢复。

通过标准：连续运行 24 小时，无 DNS 锁死、无代理循环、无残留路由。

### 阶段 1：最小可用产品（MVP）

- SwiftUI 菜单栏应用。
- 添加/删除应用规则。
- 添加/删除网站规则。
- Quickcat 端口检测。
- 启停 Mihomo。
- 生成和校验配置。
- 显示最近连接和规则命中。
- 一键恢复直连。

### 阶段 2：稳定性

- 睡眠和唤醒恢复。
- Wi-Fi、有线网络和热点切换。
- Quickcat 切节点和断线恢复。
- 配置原子更新和回滚。
- 日志轮转。
- 崩溃恢复。
- 辅助进程匹配。

### 阶段 3：体验增强

- 应用图标和拖放添加。
- 浏览器扩展或快捷方式添加当前网站。
- 规则分组。
- 临时规则。
- 导入、导出和备份。
- 规则命中统计。
- 代理延迟和可用性检测。

### 阶段 4：产品化评估

- Apple Network Extension 可行性验证。
- Developer ID 签名和公证。
- 自动更新。
- 隐私政策。
- GPL 合规审查。
- 安全审计和第三方依赖清单。

## 14. 测试计划

### 基础分流

- 未添加的网站显示本地直连 IP。
- 添加的网站显示 Quickcat 节点 IP。
- 未添加的应用保持直连。
- 添加的应用全部外部连接走 Quickcat。
- 本地开发服务器始终直连。

### 故障恢复

- Quickcat 未启动。
- Quickcat 已启动但未连接节点。
- Quickcat 切换节点。
- Mihomo 异常退出。
- 管理应用异常退出。
- Mac 睡眠、唤醒和重启。
- Wi-Fi 切换或断网重连。

### 协议覆盖

- HTTP/1.1、HTTP/2、HTTP/3。
- WebSocket。
- TCP 长连接。
- UDP。
- IPv4 和 IPv6。
- 普通 DNS、DoH 和 DoT。

### 安全检查

- 无代理循环。
- 无 DNS 泄漏。
- 无本地控制端口外部暴露。
- 无敏感请求内容日志。
- 恢复直连后无残留系统设置。
- 配置注入和恶意规则输入测试。

## 15. 主要风险

### macOS TUN 和 DNS 稳定性

macOS 大版本更新可能改变路由、权限或 DNS 行为。必须针对 macOS 26 单独测试，不能直接照搬旧版配置。

### 进程识别不完整

某些应用使用多个 Helper、XPC 服务或共享系统进程。需要允许用户查看实际连接进程并补充规则。

### UDP 支持

Quickcat 的本地 SOCKS5 端口是否完整支持 UDP 需要实测。如果不支持，QUIC、游戏和部分实时通信不能保证代理。

### DNS 和加密 DNS

使用内置 DoH 的应用可能绕过普通 DNS 劫持，需要结合目标地址、SNI 和连接日志判断。

### 网络恢复

任何会改变路由或 DNS 的工具都必须把“安全恢复网络”作为第一优先级，而不是附加功能。

### 开源许可证

Mihomo 使用 GPL-3.0。个人自用原型风险较低；如果打包分发、修改或链接相关代码，需要履行 GPL 源码和许可证义务，并在发布前进行合规审查。

## 16. 验收标准

MVP 满足以下条件才视为完成：

- 默认全部直连。
- 网站可通过界面添加并稳定走 Quickcat。
- 应用可通过选择器添加并稳定走 Quickcat。
- Quickcat 和工具自身不会产生路由循环。
- 本地及局域网访问不受影响。
- Quickcat 不可用时按用户选择阻止或直连。
- 工具退出后网络恢复正常。
- 睡眠唤醒和网络切换后能自动恢复。
- 用户能查看每条连接命中的规则和最终出口。
- 不修改 Quickcat 文件，不保存 Quickcat 凭据。

## 17. 推荐的第一步

先完成一个不带 GUI 的技术验证：

1. 安装独立 Mihomo 二进制。
2. 生成最小 TUN 配置。
3. 将 Quickcat `127.0.0.1:21080` 配成 SOCKS5 上游。
4. 将 Quickcat、Mihomo、本地和局域网加入直连规则。
5. 添加一个测试域名和一个测试应用。
6. 使用连接日志验证规则命中。
7. 测试 Quickcat 退出、睡眠唤醒和网络恢复。
8. 技术验证通过后再搭建 SwiftUI 工程。

这一阶段先证明网络路径可靠，避免在 TUN、DNS 或进程识别尚未验证时过早投入界面开发。

## 18. 参考资料

- Mihomo 项目：<https://github.com/MetaCubeX/mihomo>
- Mihomo 规则：<https://wiki.metacubex.one/en/config/rules/>
- Mihomo TUN：<https://wiki.metacubex.one/en/config/inbound/tun/>
- Mihomo SOCKS 出口：<https://wiki.metacubex.one/en/config/proxies/socks/>
- Mihomo DNS：<https://wiki.metacubex.one/en/config/dns/>
- Apple Network Extension 配置：<https://developer.apple.com/documentation/xcode/configuring-network-extensions>
- Apple Packet Tunnel 使用边界：<https://developer.apple.com/documentation/technotes/tn3120-expected-use-cases-for-network-extension-packet-tunnel-providers>
- Apple Network Extension 部署：<https://developer.apple.com/documentation/technotes/tn3134-network-extension-provider-deployment>

